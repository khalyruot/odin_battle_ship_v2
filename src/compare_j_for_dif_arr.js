const store_value_splice = require('./store_value_splice.js');

const storeValueSplice = new store_value_splice();

class compare_j_for_dif_arr{
    
    compare_j_for_dif_arr(a,b,c,d){
        let arr_ship_hit = [];
        if(a=b){
           arr_ship_hit = c.push(d);
           return arr_ship_hit;
        }
    }
}

module.exports = compare_j_for_dif_arr;